#include "main.h"

/**
 * _strlen - print the length of the string
 * @s: parameter
 * Return: length of string
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	for (i = 0; s[i] != '\0'; i++)
	{
		continue;
	}
	return (i);
}
